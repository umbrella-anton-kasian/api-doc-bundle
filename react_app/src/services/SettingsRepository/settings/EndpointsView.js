const name = 'EndpointsView';

const TYPE_SINGLE = 'Single';
const TYPE_ACCORDION = 'Accordion';

const values = {
  TYPE_SINGLE,
  TYPE_ACCORDION,
};

const defaultValue = TYPE_SINGLE;

export {
  name,
  values,
  defaultValue,
};

